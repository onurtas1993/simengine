[package]
name = "simengine"
version = "0.1.0"
edition = "2021"
license = "MIT"

[dependencies]
simengine-core = { path = "../simengine-core" }
simengine-plugin-api = { path = "../simengine-plugin-api" }
simengine-network = { path = "../simengine-network" }
anyhow = "1"
clap = { version = "4", features = ["derive"] }
libloading = "0.8"
serde_json = "1"
